from setuptools import setup

setup(
    name="topic_3",
    setup_requires=['bump2version'],
    version="{major}.{minor}.{patch}".format(**{
        "major": 1,
        "minor": 0,
        "patch": 1,
    }),
    author="Shoxrux Yuldashov",
    description="3. Переменные и объекты",
)
